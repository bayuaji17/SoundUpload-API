{
  "name": "nodejscloudstorage",
  "version": "1.0.0",
  "description": "1. Prepare a service account key\r 2. npm install\r 3. node index.js\r 4. To test the API, do curl -X POST -F "file=@audio.wav" http://localhost:5000/upload" http://localhost:5000/upload``",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^6.10.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "util": "^0.12.5"
  },
  "keywords": [],
  "devDependencies": {
  }
}
